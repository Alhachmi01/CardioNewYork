# Redis used for short-lived OGAds session verification.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional but strongly recommended.
# Set this in Vercel and add the same value to the OGAds postback URL as:
# ?pb_secret=YOUR_SECRET
OGADS_POSTBACK_SECRET=
